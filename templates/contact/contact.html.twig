{% extends 'base.html.twig' %}

{% block title %}Liste des Garagistes{% endblock %}

{% block body %}
    <h1>Liste des Garagistes</h1>

    <table class="table table-dark">
        <thead>
        <tr>
            <th>Nom d'utilisateur</th>
            <th>Email</th>
{#           <th>Roles</th>#}
        </tr>
        </thead>
        <tbody>
        {% for user in users %}
            <tr>
                <td>{{ user.userName }}</td>
                <td>{{ user.email }}</td>
{#                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>#}
            </tr>
        {% else %}
            <tr>
                <td colspan="6">Pas d'utilisateurs trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
