{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}

    {{ form_start(registrationForm) }}
        {{ form_widget(registrationForm) }}
        <input type="submit" value="S'inscrire" class="btn btn-outline-info">
    {{ form_end(registrationForm) }}

{% endblock %}
